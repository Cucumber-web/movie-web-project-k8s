apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-front
spec:
  selector:
    matchLabels:
      app: test-front
  replicas: 1
  template:
    metadata:
      labels:
        app: test-front
    spec:
      nodeName: cnp-node-w-1vme
      containers:
      - name: test-front
        image: uh2959/cucumber-front:14
        resources:
          limits:
            memory: "128Mi"
            cpu: "250m"
        ports:
        - containerPort: 80
